import { Inter, Jua } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  weight: ['500', '600'],
  variable: '--font-inter',
});

export const jua = Jua({
  subsets: ['latin'],
  display: 'swap',
  weight: '400',
  variable: '--font-jua',
});
